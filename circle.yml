machine:
  environment:
    MOCHA_FILE: "$CIRCLE_TEST_REPORTS/junit/test-results.xml"
  node:
    # Circle will use node v4.0.0
    version: 4.0.0
general:
  artifacts:
    - "$CIRCLE_TEST_REPORTS/junit/test-results.xml"
deployment:
  staging:
    branch: master
    heroku:
      appname: maps-js-lib
