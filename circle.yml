machine:
  environment:
    MOCHA_FILE: "$CIRCLE_TEST_REPORTS/test-results.xml"
  node:
    # Circle will use node v4.0.0
    version: 4.0.0
deployment:
  staging:
    branch: master
    heroku:
      appname: maps-js-lib
